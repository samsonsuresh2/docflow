spring:
  datasource:
    url: jdbc:oracle:thin:@//localhost:1522/FREEPDB1
    username: DOCFLOW
    password: Docflow#123
    driver-class-name: oracle.jdbc.OracleDriver

  jpa:
    database-platform: org.hibernate.dialect.OracleDialect
    hibernate:
      ddl-auto: none
    show-sql: true

  flyway:
    enabled: false
  docflow:
    rules:
      short-circuit: true

logging:
  level:
    root: INFO
    com.docflow: TRACE
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql: TRACE
  
    
docflow:
  upload:
    base-path: C:/Users/Samson/projects/docflow/uploads    